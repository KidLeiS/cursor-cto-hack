{
  "name": "cursor-cto-hack",
  "private": true,
  "scripts": {
    "migrate": "pnpm --dir backend migrate",
    "smoke": "node scripts/smoke.mjs",
    "test": "pnpm --dir frontend test",
    "build:frontend": "pnpm --dir frontend build"
  }
}
